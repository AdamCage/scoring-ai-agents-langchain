# Docs

- Architecture diagrams are Mermaid files in `docs/architecture/`.
- Process docs in `docs/processes/` must be self-contained and reference screenshots.
- `scripts/capture_docs.py` renders diagrams and Playwright UI shots.
- Frontend Architecture Explorer consumes the same files via `/api/docs`.
